const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export async function renderPanelSettings(container,{api,run,toast,onSaved}){
 const config=await api('settings');
 container.innerHTML=`<section class="panel"><h2>TMDB</h2><p class="muted">Clé API ou jeton de lecture TMDB pour enrichir la progression, la bibliothèque et les contenus vus avec les jaquettes et fiches françaises. La valeur est conservée chiffrée sur le serveur.</p><p>${config.tmdbConfigured?'✓ Une clé TMDB est enregistrée et l’enrichissement est actif.':'Aucune clé TMDB enregistrée.'}</p><form id="tmdb-form" class="form"><label>Clé API / API Read Access Token<input name="tmdbKey" type="password" autocomplete="off" maxlength="4096" placeholder="${config.tmdbConfigured?'Saisir une nouvelle clé pour la remplacer':'Saisir votre clé TMDB'}" required></label><div class="actions"><button class="primary">Enregistrer la clé</button>${config.tmdbConfigured?'<button type="button" id="remove-tmdb">Supprimer la clé</button>':''}</div></form><p class="muted">Les résultats sont mis en cache pendant 7 jours.</p></section><section class="panel"><h2>Trakt et Simkl</h2><p class="muted">Configure les applications utilisées pour associer un compte de suivi à chaque profil Nuvio. Les secrets restent chiffrés côté serveur.</p><form id="trackers-form" class="form"><h3>Application Trakt</h3><p class="muted">Crée une application depuis <a href="https://trakt.tv/oauth/applications" target="_blank" rel="noopener noreferrer">les applications API Trakt</a>. L’association des profils utilise ensuite le code affiché par Trakt.</p><label>Client ID<input name="traktClientId" value="${esc(config.trackers?.trakt?.clientId)}" autocomplete="off" maxlength="4096"></label><label>Client Secret<input name="traktClientSecret" type="password" autocomplete="off" maxlength="4096" placeholder="${config.trackers?.trakt?.configured?'Secret déjà enregistré — laisser vide pour le conserver':'Client Secret Trakt'}"></label><h3>Application Simkl</h3><p class="muted">Crée une application dans <a href="https://simkl.com/settings/developer/new/" target="_blank" rel="noopener noreferrer">les réglages développeur Simkl</a> et utilise <code>urn:ietf:wg:oauth:2.0:oob</code> comme URI de redirection.</p><label>Client ID / clé API<input name="simklClientId" value="${esc(config.trackers?.simkl?.clientId)}" autocomplete="off" maxlength="4096"></label><button class="primary">Enregistrer les applications</button><p class="muted">Associe ensuite Trakt ou Simkl depuis l’onglet dédié de chaque profil.</p></form></section><section class="panel"><h2>Administrateur du panel</h2><p class="muted">${config.authEnabled?'Le mot de passe actuel est requis pour modifier les identifiants.':'L’accès local est actuellement sans mot de passe. Définis un mot de passe pour activer la connexion administrateur.'}</p><form id="admin-form" class="form"><label>Nom d’utilisateur<input name="username" value="${esc(config.username)}" autocomplete="username" maxlength="80" required></label>${config.authEnabled?'<label>Mot de passe actuel<input name="currentPassword" type="password" autocomplete="current-password" required></label>':''}<label>Nouveau mot de passe<input name="newPassword" type="password" autocomplete="new-password" minlength="12" maxlength="1024" ${config.authEnabled?'placeholder="Laisser vide pour le conserver"':'required'}></label><label>Confirmer le nouveau mot de passe<input name="confirmation" type="password" autocomplete="new-password" maxlength="1024"></label><p class="muted">12 caractères minimum. Les autres sessions seront déconnectées après enregistrement.</p><button class="primary">Enregistrer les identifiants</button></form></section>`;
 const $=s=>container.querySelector(s);
 $('#tmdb-form').onsubmit=e=>{e.preventDefault();run(async()=>{await api('settings/tmdb',{key:new FormData(e.target).get('tmdbKey')});toast('Clé TMDB enregistrée.');await onSaved();});};
 if($('#remove-tmdb'))$('#remove-tmdb').onclick=()=>run(async()=>{await api('settings/tmdb',{key:''});toast('Clé TMDB supprimée.');await onSaved();});
 $('#trackers-form').onsubmit=e=>{e.preventDefault();run(async()=>{const f=Object.fromEntries(new FormData(e.target));await api('settings/trackers',f);toast('Applications Trakt et Simkl enregistrées.');await onSaved();});};
 $('#admin-form').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target);run(async()=>{if(f.get('newPassword')!==f.get('confirmation'))throw Error('Les deux mots de passe ne correspondent pas.');await api('settings/admin',{username:f.get('username'),currentPassword:f.get('currentPassword')||'',newPassword:f.get('newPassword')});toast('Identifiants administrateur enregistrés.');await onSaved();});};
}
